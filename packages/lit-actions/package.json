{
  "name": "@swarm-vault/lit-actions",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "esbuild src/signTransaction.ts --bundle --outfile=dist/signTransaction.js --format=iife --platform=neutral",
    "dev": "pnpm build --watch",
    "lint": "eslint . --ext ts --report-unused-disable-directives --max-warnings 0"
  },
  "devDependencies": {
    "@lit-protocol/types": "^6.0.0",
    "esbuild": "^0.19.11",
    "typescript": "^5.3.3"
  }
}
